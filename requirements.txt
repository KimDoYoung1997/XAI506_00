# =============================================================================
# HF notebooks: 01_hf_sam2, 02_hf_depth_anything, 03_hf_smolvlm
#
# Python: 3.10–3.12 권장 (open3d 등 선택 패키지는 PyPI 휠이 OS·버전별로 다름).
#
# 한 번에: conda activate … 후
#   pip install -r requirements.txt
#
# NVIDIA GPU용 CUDA 휠이 필요하면 [PyTorch 시작하기](https://pytorch.org/get-started/locally/)
# 에서 받은 index-url로 torch/torchvision만 먼저(또는 덮어) 설치한 뒤, 나머지는 동일하게 설치.
# =============================================================================

# --- PyTorch (세 노트북 공통; 기본은 PyPI 휠 — Mac MPS·CPU·대부분 Linux에 적합) ---
torch>=2.1.0
torchvision>=0.16.0

# --- HF / 딥러닝 (SAM2, Depth Anything V2, SmolVLM2 공통) ---
transformers>=4.50.0
accelerate>=1.0.0
safetensors>=0.4.0
huggingface-hub>=0.20.0

# --- 수치 / 이미지 / 시각화 (공통) ---
numpy>=1.26.0,<3.0.0
pillow>=10.0.0
matplotlib>=3.8.0

# --- 01_hf_sam2: scipy (마스크 후처리), PyQt5 (포인트 픽커) ---
scipy>=1.11.0
PyQt5>=5.15.0

# --- 03_hf_smolvlm: SmolVLM2 프로세서가 import 시 요구 ---
num2words>=0.5.0

# --- Jupyter (노트북 열기·커널 등록) ---
notebook>=7.0.0
ipykernel>=6.29.0

# =============================================================================
# 선택 사항 (해당 기능을 쓸 때만 설치)
# =============================================================================

# 02: 마크다운의 Open3D·3D 시각화 확장용. 깊이 맵·2D 플롯만이면 필수 아님.
# open3d>=0.18.0

# =============================================================================
# 고정 버전으로 맞추려면 (예시 — torch/torchvision은 환경에 맞게 조정)
#   pip install torch==… torchvision==…
#   pip install transformers==4.57.3 accelerate==1.12.0 numpy==2.2.6 pillow==12.1.1 matplotlib==3.10.8
#   pip install scipy==1.15.3 PyQt5==5.15.11 num2words==0.5.14 ipykernel==6.29.5 notebook==7.5.1
# =============================================================================
